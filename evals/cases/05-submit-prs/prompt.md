Implement a two-part change under `eval/{run_id}/`: `data.js` exports a small data object and
`consumer.js` imports it. Use `{default_branch}` as the stack trunk. Put each part on its own
dependent branch named `{branch_prefix}/data` and `{branch_prefix}/consumer`, then open both pull
requests ready for review. Work autonomously.
