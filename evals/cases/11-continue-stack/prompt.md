Continue the current stack with a new top layer named `{branch_prefix}/audit`. Add
`eval/{run_id}/audit.js`, which imports `page.js` and exports `auditUser(user)`. Commit the new
layer, remain on it, and do not push. Work autonomously.
