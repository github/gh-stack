Implement a complete three-part user feature under `eval/{run_id}/`: `model.js` exports
`normalizeUser(user)`, `api.js` imports it and exports `serializeUser(user)`, and `page.js`
imports the serializer and exports `renderUser(user)`. Keep each concern isolated so the work is
easy to review. If you create branches, use `{default_branch}` as the trunk and prefix every
branch with `{branch_prefix}/`. Implement and commit the work; do not push or open pull requests.
Work autonomously and finish with a concise result.
