Branches `{branch_prefix}/base` and `{branch_prefix}/top` already form a dependency chain but are
not tracked by gh-stack. Link them into a GitHub Stack with `{default_branch}` as the ultimate base,
open both pull requests ready for review, and do not create local gh-stack tracking. Work
autonomously.
